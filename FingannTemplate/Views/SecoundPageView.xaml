<UserControl x:Class="FingannTemplate.Views.SecoundPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FingannTemplate.Views"
             mc:Ignorable="d"
             DataContext="{Binding SecoundPage, Source={StaticResource Locator}}"
             
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Button Command="{Binding GotoMainPageCommand}">Go to Main page</Button>
    </Grid>
</UserControl>
